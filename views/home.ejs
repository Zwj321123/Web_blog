<%- include('partials/header'); %>
<h1>Home</h1>
<% posts_html.forEach(function(post){ %>
    <h2><%= post.title %></h2>
    <p><%= post.body.substring(0, 100) + "..."; %></p>
    <a href="/posts/<%= post.title %>">Read more</a>
<% }); %>
<%- include('partials/footer'); %>